name: A workflow to say hello

on: [ push ]

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Say Hello
      run: echo "Hello, world!"